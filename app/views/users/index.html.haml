%h1 Listing users

%table
  %tr
    %th First name
    %th Last name
    %th Phone number
    %th Contact
    %th Banned
    %th Address
    %th Zip code
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.first_name
      %td= user.last_name
      %td= user.phone_number_id
      %td= user.contact_id
      %td= user.banned
      %td= user.address
      %td= user.zip_code
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New User', new_user_path
